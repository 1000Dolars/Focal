node_modules/
.expo/
dist/
dist-*/
web-build/
*.log
npm-debug.*
.DS_Store
*.orig.*
.env*.local

# Native projects generated by `expo prebuild` — regenerate them, don't commit
# them. Keeping them out avoids drift between app.json and the native files.
/android/
/ios/

# Machine-specific and build output (harmless if the folders above are ignored,
# listed so a bare-workflow switch stays clean).
android/local.properties
android/.gradle/
android/app/build/
.gradle/
